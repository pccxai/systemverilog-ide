# Contributing

Contributions are reviewed through pull requests.

Before opening a pull request:
- Keep the change scoped to one concern.
- Include tests or validation notes when behavior changes.
- Update documentation when public behavior or setup changes.
- Avoid unsupported performance, hardware, runtime, or release claims.

Security issues should be reported privately as described in `SECURITY.md`.
